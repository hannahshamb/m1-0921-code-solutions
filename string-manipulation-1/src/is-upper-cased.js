/* exported isUpperCased */
function isUpperCased(word) {
  if (word.toUpperCase() === word) {
    return true;
  } else return false;
}

// define a function named isUpperCased that takes a single parameter "word"
// create an if statement with an expression that checks if the uppercased version of the word is strictly equal to its original state
// if that expression is true, return true
// if that expression is false, return false
