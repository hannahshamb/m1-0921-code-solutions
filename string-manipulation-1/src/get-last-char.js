/* exported getLastChar */
function getLastChar(string) {
  return string.slice(-1);
}
// define a function named getLastChar that takes one parameter, "string"
// slice the string at a negative one value to cut off the rest of the characters aside from the last character, which is then returned
