/* exported getFirstChar */
function getFirstChar(string) {
  return string.charAt(0);
}

// define a function called getFirstChar that takes 1 parameter "string"
// return the value of the string at index 0 (which pulls the first character of the string)
