/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// define a function that takes 2 parameters, 'object' and 'key'
// return the value of the parameter 'object' associated with the parameter 'key's property
