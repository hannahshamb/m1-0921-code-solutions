/* exported head */
function head(array) {
  return array[0];
}

// define a function called head that takes a single parameter 'array'
// return the array at the 0 index which returns the first element of the array
