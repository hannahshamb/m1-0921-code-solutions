/* exported last */
function last(array) {
  return array[array.length - 1];
}

// define a function called last that takes a single parameter 'array'
// return the array at the last index of the array which returns the last element of the array
